# Gmail Real-time Threat Detection System - Requirements
# Simplified version without Redis/MongoDB dependencies

# Core ML and Data Science
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
scipy==1.11.1
joblib==1.3.1

# NLP and Text Processing
nltk==3.8.1
# Removed spacy - not essential for core functionality

# Google APIs
google-auth==2.22.0
google-auth-oauthlib==1.0.0
google-auth-httplib2==0.1.0
google-api-python-client==2.88.0

# Web Framework and APIs
flask==2.3.2
requests==2.31.0

# Utilities
python-dotenv==1.0.0
colorama==0.4.6
tqdm==4.65.0
gunicorn==23.0.0

# Built-in Python libraries used (no installation needed):
# - sqlite3 (for data storage)
# - json (for data serialization)
# - threading (for concurrent processing)
# - datetime (for timestamps)
# - logging (for system logs)
